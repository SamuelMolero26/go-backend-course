## Learning Notes on Front-End

State is a memory of the component --> Values that change over time and when they change they reload the UI.

- State Notifies when it changes
- Local to the component (enclosed to the component)
  - Each Component has its own private memory
- React forces you to change in an inmutable way (create a new value, cant modify the old one)